# Analyzer2

## Start
```sh
# Start containers
$ docker-compose up -d

# See logs
$ docker-compose logs

# Stop
$ docker-compose stop

# To scale
$ docker-compose scale analyzer=2
$ docker-compose up --force-recreate -d
$ docker-compose logs
```
